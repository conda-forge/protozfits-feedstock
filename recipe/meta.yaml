{% set name = "protozfits" %}
{% set version = "2.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 640d8405fdfdcfb225577548df504eba042a21fc062c1d85908aca48f8aec225
  patches:
    - 0001-Add-support-for-CMAKE_ARGS-in-setup.py-to-enable-con.patch

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  skip: true  # [win]
  skip: true  # [py<36]
  skip: true  # [python_impl == 'pypy']

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake
    - make   # [not win]
    - ninja  # [win]
    # for enabling m1 cross compilation
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - protobuf =3.20                         # [build_platform != target_platform]
    - libprotobuf =3.20                      # [build_platform != target_platform]
    - setuptools                             # [build_platform != target_platform]
    - zeromq                                 # [build_platform != target_platform]
    - zlib                                   # [build_platform != target_platform]
  host:
    - pip
    - python
    - protobuf =3.20
    - libprotobuf =3.20
    - setuptools
    - zeromq
    - zlib
  run:
    - astropy
    - numpy
    - protobuf =3.20
    - python
    - zeromq
    - zlib

test:
  imports:
    - protozfits
    - protozfits.rawzfits
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://gitlab.cta-observatory.org/cta-computing/common/acada-array-elements/adh-apis
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Python bindings the CTA zfits reader
  description: |
    protozfits are the python bindings for reading the protozfits raw data format,
    which is a prototype raw data format build in FITS and Protocol Buffers for
    the data of the Cherenkov Telescope Array.
  dev_url: https://gitlab.cta-observatory.org/cta-computing/common/acada-array-elements/adh-apis

extra:
  recipe-maintainers:
    - maxnoe
    - dneise
